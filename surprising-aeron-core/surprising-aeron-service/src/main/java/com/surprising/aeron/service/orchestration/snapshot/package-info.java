/**
 * Snapshot metadata exposed across the runtime boundary. Snapshot encoding, recovery, and
 * validation remain with the owner-side implementation until their state access is explicit.
 */
package com.surprising.aeron.service.orchestration.snapshot;

/**
 * Versioned instrument configuration and product-rule input values.
 *
 * <p>Instrument values are immutable. Instrument lifecycle transitions and
 * the authoritative instrument map remain owned by the state runtime.</p>
 */
package com.surprising.aeron.service.state.instrument;

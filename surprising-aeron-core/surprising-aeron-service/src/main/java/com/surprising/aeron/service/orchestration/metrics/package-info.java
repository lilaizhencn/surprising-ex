/**
 * Immutable operational metrics returned by the trading runtime. Metrics are observation
 * views and are not a second source of account, order, or position state.
 */
package com.surprising.aeron.service.orchestration.metrics;

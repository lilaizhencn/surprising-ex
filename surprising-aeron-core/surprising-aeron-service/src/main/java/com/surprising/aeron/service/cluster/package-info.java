/**
 * Aeron Cluster 节点启动和运行时拓扑配置。
 *
 * <p>本包只负责 MediaDriver、Archive、ConsensusModule、ClusteredServiceContainer 以及
 * 节点级错误处理。订单、余额、持仓、撮合和产品线规则不属于本包。</p>
 */
package com.surprising.aeron.service.cluster;

# 交易核心文档入口

当前实现和后续重构唯一依据：

- [高性能交易核心最终实施规格](high-performance-trading-core-implementation.md)

该规格合并并继续追踪此前 Aeron 统一交易核心、exchange-core 单一盘口、账户单写者、产品线隔离、
恢复、导出、性能、脚本治理、Risk/Instrument 唯一来源和资金对账方案中的约束、问题清单、阶段台账和
验收门禁。历史文档即使从工作树移除，相关设计要求也以该规格第 3.6、13–18 节为准；不得因为旧文件
删除而丢失约束。

当前路线：先完成 `P0-DOC-AUDIT` 和基线 manifest，再严格按 P1 → P2 → P3 → P4 → P5 → P6 实施。每个
阶段都必须同时更新代码、测试、脚本、状态台账和证据，未具备运行时证据不得标记 `DONE`。

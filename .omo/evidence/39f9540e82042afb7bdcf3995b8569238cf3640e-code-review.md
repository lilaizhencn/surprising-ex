# Code review — `39f9540e82042afb7bdcf3995b8569238cf3640e`

## recommendation

APPROVE with non-blocking notes.

## scope

Exact commit `39f9540e82042afb7bdcf3995b8569238cf3640e` against parent `bb722fe57bb3a31b7603962c0ee642e7929a7f9a`; changed files are `src/App.tsx` and `test/market-presentation.test.ts`.

## functional review

Realtime product isolation passes. `src/App.tsx:583-598` admits only events matching `activeProductLine`; trades, index, mark, and funding pass `activeProductMode` at lines 619, 627, 641, and 650; `patchMarket` requires matching symbol plus product at lines 655-662.

## findings

- MEDIUM/NOTE: `test/market-presentation.test.ts:56-60` duplicates the favorite-key assertions at lines 6-13 and 21-27. It does not exercise realtime mutation and would pass if this commit's production fix were reverted. Replace it with a behavior-level duplicate-symbol realtime regression.
- MEDIUM/NOTE: `src/App.tsx` is a pre-existing oversized module (3,177 pure LOC). This is maintenance debt, not a correctness failure introduced by this narrow fix.

## remove-ai-slops and programming coverage

Directly applied both skill perspectives. The added test is useless/overfit and creates false confidence. The production diff adds no needless extraction, parsing, normalization, dead code, `any`, `@ts-ignore`, `@ts-expect-error`, or unnecessary abstraction. The existing oversized component and missing focused regression are maintenance/testing notes, not failures of the stated product-isolation criterion.

## validation

- `git diff --check HEAD^ HEAD`: PASS.
- `npm test`: PASS, 15/15.
- Exact-commit archive: `pnpm lint` PASS; `pnpm build` PASS.
- Browser smoke: Playwright CLI loaded Chrome at `/markets` and captured `/tmp/sx-39f9540-markets.png`.


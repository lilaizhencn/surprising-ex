# Clone / Design-System Fidelity Review — `39f9540e82042afb7bdcf3995b8569238cf3640e`

## Recommendation

**REQUEST_CHANGES (FAIL)**

The exact object is `HEAD` of the sibling frontend checkout `surprising-ex-web`; its tracked worktree is clean. It uses live React DOM and shared primitives, not a screenshot or background-image surrogate. Approval is blocked by token-contract violations, an incomplete mobile Home path, inaccurate ranking/count semantics for halted instruments, and missing production-live/reference evidence.

## Findings

### CRITICAL

None. `MarketsPage` renders live semantic controls. It composes `UiCard`, `UiButton`, `UiStatusBadge`, and a reused `MarketRow`; no raster, canvas, or CSS background stands in for the interface.

### HIGH

1. **Not fully token-driven.** The market screen embeds literal geometry/type/elevation values rather than using the documented `--sx-*` contract: `1280px`/`48px` in the page frame at [src/styles.css:4067](/Users/atomex/Desktop/surprising/surprising-ex-web/src/styles.css:4067), `clamp(30px, 4vw, 44px)` at [src/styles.css:4070](/Users/atomex/Desktop/surprising/surprising-ex-web/src/styles.css:4070), `320px` search width at [src/styles.css:4073](/Users/atomex/Desktop/surprising/surprising-ex-web/src/styles.css:4073), raw focus-ring values at [src/styles.css:4074](/Users/atomex/Desktop/surprising/surprising-ex-web/src/styles.css:4074), and `36px` controls at [src/styles.css:4078](/Users/atomex/Desktop/surprising/surprising-ex-web/src/styles.css:4078).

2. **Mobile nav loses Home.** Desktop has `首页` at [src/App.tsx:1777](/Users/atomex/Desktop/surprising/surprising-ex-web/src/App.tsx:1777), but the 375px product menu at [src/App.tsx:1832](/Users/atomex/Desktop/surprising/surprising-ex-web/src/App.tsx:1832)-[1838](/Users/atomex/Desktop/surprising/surprising-ex-web/src/App.tsx:1838) omits it. The logo’s text is hidden at [src/styles.css:4328](/Users/atomex/Desktop/surprising/surprising-ex-web/src/styles.css:4328)-[4330](/Users/atomex/Desktop/surprising/surprising-ex-web/src/styles.css:4330), while the icon-only brand button has no explicit accessible label at [src/App.tsx:1772](/Users/atomex/Desktop/surprising/surprising-ex-web/src/App.tsx:1772). The fresh production accessibility snapshot reported the button unnamed.

3. **Metrics/rankings can call halted markets tradable.** `MarketsPage` filters only `tickerReady` at [src/components/MarketsPage.tsx:36](/Users/atomex/Desktop/surprising/surprising-ex-web/src/components/MarketsPage.tsx:36) before computing rising count, total volume, and both leaderboards at [src/components/MarketsPage.tsx:37](/Users/atomex/Desktop/surprising/surprising-ex-web/src/components/MarketsPage.tsx:37)-[40](/Users/atomex/Desktop/surprising/surprising-ex-web/src/components/MarketsPage.tsx:40). A `HALTED` market with ticker data remains eligible. The home ranking excludes `HALTED` at [src/App.tsx:1212](/Users/atomex/Desktop/surprising/surprising-ex-web/src/App.tsx:1212), so metric scope is inconsistent.

### MEDIUM

1. **Live production data was unavailable.** Fresh exact-build captures at 1280px and 375px showed the truthful unavailable state, zero rows, and no table/rankings. The six-row filter/ranking/table exercise was possible only on the labelled DEV fallback; it is not proof for a real gateway response.

2. **No trusted reference packet was supplied for this commit.** Existing screenshots/manual-QA artifacts concern prior revisions or do not establish a pixel target for `39f9540…`, so reference visual fidelity is not verifiable.

### LOW

1. The DEV server injects a `react-grab` inspection toolbar via [src/main.tsx:5](/Users/atomex/Desktop/surprising/surprising-ex-web/src/main.tsx:5)-[7](/Users/atomex/Desktop/surprising/surprising-ex-web/src/main.tsx:7), visibly covering content in the fresh `5174` captures. The production preview has no toolbar; nevertheless, it invalidates unobstructed dev visual captures unless disabled for QA.

## Verified behavior

- Exact diff: four realtime `patchMarket` calls now pass `activeProductMode`; the guard is implemented at [src/App.tsx:655](/Users/atomex/Desktop/surprising/surprising-ex-web/src/App.tsx:655)-[663](/Users/atomex/Desktop/surprising/surprising-ex-web/src/App.tsx:663).
- 375px and 1280px pages have no document-level horizontal overflow. At 375px only the table scrolls horizontally (`700px` table content in a `349px` viewport), via [src/styles.css:4108](/Users/atomex/Desktop/surprising/surprising-ex-web/src/styles.css:4108) and [src/styles.css:4122](/Users/atomex/Desktop/surprising/surprising-ex-web/src/styles.css:4122)-[4125](/Users/atomex/Desktop/surprising/surprising-ex-web/src/styles.css:4125).
- DEV fallback populated rankings sort correctly: change `+1.86%, +1.58%, +1.23%`; volume `486.21M, 318.9M, 220.89M`. Searching `现货` returned exactly the two spot rows.
- `npm test` passed 15/15; `npm run lint` and `npm run build` passed.

## Blockers before approval

1. Replace market literals with documented/additional `--sx-*` tokens.
2. Restore a visible and explicitly accessible mobile Home path.
3. Exclude `HALTED` instruments from tradable counts/rankings (or rename the metric) and add regression coverage.
4. Provide production-like live data and trusted 375px/1280px reference screenshots, then re-run pixel comparison without the DEV overlay.

## Evidence inspected

- Exact commit/diff in `/Users/atomex/Desktop/surprising/surprising-ex-web`.
- `DESIGN.md`, `src/App.tsx`, `src/components/MarketsPage.tsx`, `src/components/UiPrimitives.tsx`, `src/marketPresentation.ts`, `src/styles.css`, `src/main.tsx`, and `test/market-presentation.test.ts`.
- Fresh screenshots and geometry: `/Users/atomex/Desktop/surprising/surprising-ex-web/.omo/evidence/39f9540e-fidelity/production-markets-1280.png`, `production-markets-375.png`, `production-mobile-products-375.png`, and DEV fallback counterparts in that directory.
- No notepad path or commit-specific target/reference screenshot packet was supplied or found.
